version: '3.8'
services:
  mongodb:
    image: mongo:7.0
    container_name: mongo-container
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
      - ./import:/import